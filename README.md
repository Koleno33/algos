# algos
Implementation of data structures and algorithms in C from scratch.

# Implementation method
An idea for an algorithm/data structure was taken, after which attempts were made to implement it in one way or another.

The repository may contain unoptimized code. The main idea is to try different solutions to recognize your mistakes and learn.
