# sllu
Singly linked list with user-side nodes creation.
The user must call `create_node` each time and use the resulting node to insert it into the list in one way or another.

The first approach that came to mind—however, it is inconvenient to use.
